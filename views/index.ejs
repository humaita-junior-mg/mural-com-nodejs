<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mural</title>
</head>
<body>

    <h1>Mural</h1>

    <a href="add">Adicionar</a>
    
    <%docs.forEach(doc=>{%>
        <form action="/<%=doc._id%>?_method=DELETE" method="POST">
            <h3><%=doc.title%></h3>
            <p><%=doc.description%></p>
            <p>Clicks: <%=doc.click%></p>
            <button type="submit">Delete</button>
            <a href="/re/<%=doc._id%>" target="_blank">Redirect</a>
            <a href="/edit/<%=doc._id%>">Edit</a>
        </form>
    <%})%>
        
</body>
</html>